#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main(){
    int N,i,res; 
    scanf("%d",&N);
    
    for(i=1;i<11;i++){
    	res=(N*i);
    	printf("%d x %d = %d\n",N,i,res);
    }
    
    return 0;
}
