#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main(){
    int n,a,i,sum=0,number; 
    scanf("%d",&n);
    int arr[n];
    for(i = 0;i < n;i++){
       scanf("%d",&number);
       sum = sum + number;
    }
    printf("%d",sum);
    return 0;
}
